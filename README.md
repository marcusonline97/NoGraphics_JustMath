# NoGraphics_JustMath
Renders a visual effect using just mathematical equations 

it will generate some .ppm files, each output is a frame that it generates. You might need to install something like [ FFmpeg](https://github.com/FFmpeg/FFmpeg) to stitch the frames into a mp4.
